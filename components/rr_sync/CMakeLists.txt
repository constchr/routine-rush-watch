# rr_sync — sync engine — pull/push/reconcile (spec Phase 5)
# SCAFFOLD: no sources yet. Add them to SRCS as the module is implemented.
idf_component_register(
    INCLUDE_DIRS "include"
    REQUIRES rr_store rr_ble
)
