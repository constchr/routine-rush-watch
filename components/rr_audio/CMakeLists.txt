# rr_audio — ES8311 effect/jingle playback (spec Phase 8)
# SCAFFOLD: no sources yet. Add them to SRCS as the module is implemented.
idf_component_register(
    INCLUDE_DIRS "include"
    REQUIRES esp_driver_i2s
)
