# rr_steps — QMI8658 pedometer + daily count (spec Phase 9)
# SCAFFOLD: no sources yet. Add them to SRCS as the module is implemented.
idf_component_register(
    INCLUDE_DIRS "include"
    REQUIRES nvs_flash
)
