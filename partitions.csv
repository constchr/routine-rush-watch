# Routine Rush Watch — 16MB flash layout (spec §2 "Partitions")
#
#   nvs       24K   device_id, child_id, settings, daily-step state.
#                   No wifi credentials are ever stored — wifi is off (§2.2).
#   otadata    8K   active-slot record
#   phy_init   4K
#   factory    3M   recovery image
#   ota_0      3M   |  OTA arrives over BLE from the parent app, so updates are
#   ota_1      3M   |  slow and rare. Keeping a factory slot means a bad OTA is
#                   |  always recoverable without a cable.
#   littlefs   7M   cached routine JSON + the durable completion queue
#                   (§5, §6.3) + emoji/audio assets (emoji alone is ~4MB).
#
# Totals: 0x920000 + 0x6E0000 = 0x1000000 = 16MB exactly.
#
# NOTE: do not put trailing "# ..." comments after the last column below —
# the partition tool parses them as the flags field and the build fails.
#
# Name,      Type, SubType,  Offset,    Size
nvs,         data, nvs,      0x9000,    0x6000
otadata,     data, ota,      0xF000,    0x2000
phy_init,    data, phy,      0x11000,   0x1000
factory,     app,  factory,  0x20000,   0x300000
ota_0,       app,  ota_0,    0x320000,  0x300000
ota_1,       app,  ota_1,    0x620000,  0x300000
littlefs,    data, spiffs,   0x920000,  0x6E0000
